# Mobile App API Usage — Month 2

## Week 1 (Week 5)

> **No new mobile-facing APIs.**
> All endpoints added (`GET /v1/dashboard/met/latest`, `GET /v1/dashboard/met/windrose`, `GET /v1/dashboard/summary`, `GET /v1/dashboard/devices`) are consumed by the Angular admin panel only.

---

## Week 2 (Week 6)

> **No new mobile-facing APIs.**
> All endpoints added (`GET /v1/dashboard/met/history`, `GET /v1/dashboard/nep/sessions`, `GET /v1/dashboard/nep/latest`, `GET /v1/dashboard/nep/trend`, `GET /v1/dashboard/nep/map`) are consumed by the Angular admin panel only.

---

Mobile app sync APIs are unchanged from Month 1 — refer to [`deliveryreport-month1/MOBILE_API_USAGE.md`](../deliveryreport-month1/MOBILE_API_USAGE.md).
