import { DirectoriesPipe } from './directories.pipe';

describe('DirectoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new DirectoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
