# The API's settings are documented in one place:
#   ../config/standalone.env.example
#
# For development, copy that file to .env here and point MONGO_URI at the local
# development database (see ../docs/BASELINE.md).
