{
  "name": "observator-provision-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Job-polling provisioning agent for the Observator SFTP box. Outbound only.",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "dev": "ts-node src/main.ts",
    "test": "node --test --require ts-node/register src/safety.test.ts src/runner.test.ts src/cross-layer.test.ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
