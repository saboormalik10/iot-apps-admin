# Local dev / build env for admin-web (Month 11 verification).
# Backend runs locally against the real Atlas cluster (backend/.env) on :3000.
BACKEND_URL=https://iot-apps-backend.vercel.app/v1
NEXT_PUBLIC_BACKEND_WS_URL=ws://iot-apps-backend.vercel.app
SESSION_SECRET=month11_local_dev_session_secret_key_0123456789
SESSION_IDLE_MINUTES=30
NEXT_PUBLIC_FEATURE_FLAGS=
